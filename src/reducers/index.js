//define the root reducer here so that we can use the combineReducers function in createStore
import {combineReducers} from 'redux'
import catsReducer from './cats_reducer'

const rootReducer

export default combineReducers({
  catsReducer,

})
